[variables]
NODE_VERSION = "20.18.0"

[phases.setup]
nixPkgs = ["nodejs_20", "npm", "python3", "gcc", "pkg-config"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "npm start"